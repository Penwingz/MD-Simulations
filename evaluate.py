"""
Evaluation entry point for the HDNNP pipeline.

Loads a checkpoint and runs the test set, reporting per-target MAE
in physical units (eV, Debye).

Usage:
    python evaluate.py --config configs/default.yaml --ckpt checkpoints/best.ckpt
"""

raise NotImplementedError(
    "evaluate.py is not yet implemented. "
    "Complete a full training run first (see train.py)."
)
